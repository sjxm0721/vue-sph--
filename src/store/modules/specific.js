import {reqActivityList,reqAddOrUpdateChildrenAttr} from '@/api/activity/specific'

const state={};

const mutations={};

const actions={
    
};

export default{
    namespaced: true,
    state,
    mutations,
    actions
}